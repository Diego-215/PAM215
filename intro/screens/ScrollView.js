import { Text, StyleSheet, View } from 'react-native'
import React, { useState } from 'react'

export default function ScrollScreen () {

    return (
      <View>
        <Text>Proximamente por Monserrath...</Text>
      </View>
    )
}

const styles = StyleSheet.create({})