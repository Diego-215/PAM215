import { Text, StyleSheet, View } from 'react-native'
import React, { useState } from 'react'

export default function FlatScreen () {

    return (
      <View>
        <Text>Proximamente por Diego Espindola...</Text>
      </View>
    )
}

const styles = StyleSheet.create({})